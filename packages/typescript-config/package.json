{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "echo 'TypeScript config package - no linting needed'",
    "lint:fix": "echo 'TypeScript config package - no linting needed'"
  },
  "publishConfig": {
    "access": "public"
  }
}
